//  // Obtener los elementos del formulario
//  var email = document.getElementById("email");
//  var password = document.getElementById("password");
//  var btn = document.getElementById("btn");

//  // Función para verificar los campos y habilitar/deshabilitar el botón
//  function verificarCampos() {
//      if (email.value.trim() !== "" && password.value.trim() !== "") {
//          btn.disabled = false; // Habilitar el botón
//      } else {
//          btn.disabled = true; // Deshabilitar el botón
//      }
//  }

//  // Agregar event listeners para los campos
//  email.addEventListener("input", verificarCampos());
//  password.addEventListener("input", verificarCampos());

//  // Llamar a verificarCampos inicialmente en caso de que los campos tengan algún valor preexistente
//  verificarCampos();

